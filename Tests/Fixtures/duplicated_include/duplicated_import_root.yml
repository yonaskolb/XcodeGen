name: DuplicatedImportRoot
fileGroups:
  - First
  - Second
targetTemplates:
  IncludedTemplate:
    type: application
    platform: iOS
    sources:
      - template
    preBuildScripts:
      - script: swiftlint
        name: Swiftlint
