name: AnotherProject
include: [../environments.yml]
configFiles:
  Test Debug: ../Configs/config.xcconfig
targets:
  BundleX:
    type: bundle
    platform: iOS
  ExternalTarget:
    type: framework
    platform: iOS
